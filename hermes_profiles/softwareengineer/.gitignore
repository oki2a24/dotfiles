# --- Hermes Agent Profile State & Environment ---

# 1. Memory & Intelligence (Highly dynamic)
memories/
*.memory
*.lock

# 2. Session, Logs & History (Large files and high frequency updates)
sessions/
logs/
*agent.log
*update.log
*error.log
state-snapshots/

# 3. Runtime State & Databases (Binary & Sensitive data)
state.db*
*.db
*.sqlite3
auth.json
auth.lock
.env
.hermes_history
.skills_prompt_snapshot.json
interrupt_debug.log
cache/
image_cache/
provider_models_cache.json
context_length_cache.yaml
ollama_cloud_models_cache.json
models_dev_cache.json

# 4. Environment, Dependencies & Runtime Binaries (Infrastructure-as-Code principles)
bin/
lsp/
node_modules/
sandboxes/
pastes/
pairing/

# 5. OS / System Files
.DS_Store
